services:
  library-maintenance-api:
    build: 
      context: .
      dockerfile: library-maintenance-api/Dockerfile
    ports:
      - 6000:6000
    env_file:
      - .env

  library-maintenance-ui:
    build: 
      context: .
      dockerfile: library-maintenance-ui/Dockerfile
    ports:
      - 80:80
    env_file:
      - .env